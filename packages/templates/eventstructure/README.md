# Event Structure

Minimal landing template: a single fullscreen 16:9 hero image on a dark backdrop. No public navbar or footer — operators reach admin only via direct links (`/admin`, `/admin/cms/home`).

Edit the hero image at **/admin/cms/home** (surface JSON visual editor).
