# Kerámia Dental — Fogfehérítés

Single-page landing template for the fogfehérítés subdomain.

Deployment: `keramia-dental` with `TEMPLATE_PIN=keramia-fogfeherites` (container) or host `fogfeherites.keramiadental.hu`.

CMS: `/admin/cms/home` (surface JSON — click-to-edit on the live page).
