# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules/
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# Local DB dumps (t-book / ops)
/backups/

# next.js
.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# Portainer / local stack with secrets (never commit)
portainer.stack.yml
portainer.stack.*.yml
docker-compose.local.yml
docker-compose.*.local.yml
# Local deployment inventory (domains, GHCR tags, stacks — operator-maintained)
deployments.registry.local.md

.v1.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/src/generated/prisma
/uploads
/uploads/*

.krausz

# Agent CMS import working files (keep *.example in repo)
/scripts/imports/customer-copy.txt
/scripts/imports/payload.json

/landing-templates
/landing-templates/*
/landing-templetes
/landing-templetes/*

# Standalone site exports (large media + generated Next apps)
/exports/
scripts/seed/backups/
